import React from 'react';
import './Resume.css';

const Resume = () => {
    return (
        <footer className="Resume">
            <p>Resume</p>
        </footer>
    );
};

export default Resume;
